const std = @import("std");

pub fn main() !void {
    // CLI stub — full implementation in Phase 1.5
    // Zig 0.15 I/O API is significantly different; deferring CLI to after core lib is solid
}

test {
    // CLI tests will be added when CLI is implemented
}

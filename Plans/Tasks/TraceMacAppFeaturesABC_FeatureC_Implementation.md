Prompt:
Implement Feature C (search/filter toolbar wiring) to satisfy tests.
Goal:
Bind toolbar controls to a typed filter model in the view model and apply filters to the span list pipeline.
Task Breakdown:
- Bind toolbar controls to filter model properties.
- Apply filter state to span list data source/derived view model outputs.
- Ensure selection invariants still hold when filtering.
Expected Output:
- Updated view(s)/view model(s) implementing filter wiring.
- Tests from Feature C pass.

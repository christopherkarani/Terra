Prompt:
Add Swift Testing tests for Feature C (search/filter toolbar wiring).
Goal:
Define behavior-driven tests verifying toolbar inputs update typed filter state and that filtering affects span list results.
Task Breakdown:
- Identify or define the filter model type in the view model.
- Add a new Swift Testing file focused on filter state updates and derived list results.
- Test toolbar input updates filter state.
- Test filter state affects span list data pipeline.
Expected Output:
- New/updated Swift Testing file(s) with failing tests for Feature C.
- Notes on any required seams to observe filtered results.
